#include <iostream>
#include <algorithm>
#include <string>
#include <vector>

using std::cin;
using std::cout;
using std::vector;
using std::string;
using std::endl;

string LCS(const string& s, const string& t) {
    // TODO: your code here
    return "";
}

int main() {
    string s,t;
    cin >> s >> t;
    cout << LCS(s,t) << endl;
}
