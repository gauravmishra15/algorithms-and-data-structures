import java.util.Scanner;

public class MultipleAlignment {
    /**
     * @param r - the first string to be aligned
     * @param s - the second string to be aligned
     * @param t - the third string to be aligned
     *
     * @return a string with the alignment score and reconstructed 
     * alignment in the correct format for this problem
     */
    private static String align(Sting r, String s, String t) {
        // TODO: your code here
        return "";
    }

    public static void main(String[] args) {
        Scanner stdin = new Scanner(System.in);
        String r = stdin.nextLine();
        String s = stdin.nextLine();
        String t = stdin.nextLine();

        System.out.println(align(r,s,t));
    }
}
