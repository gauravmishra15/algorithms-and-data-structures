#include <iostream>
#include <string>
#include <algorithm>

using std::cout;
using std::cin;
using std::endl;
using std::string;

/**
 * r, s, and t are the strings to be aligned
 *
 * return a string with the alignment score and the reconstructed alignment
 * be sure to follow the problem's output format
 */
string align(const string& r, const string& s, const string& t) {
    // TODO: your code here
    return "";
}


int main() {
    int m,mu,sigma;
    string r,s,t;
    getline(cin, r);
    getline(cin, s);
    getline(cin, t);

    cout << align(r,s,t) << endl;
    return 0;
}
